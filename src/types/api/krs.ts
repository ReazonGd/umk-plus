export interface DetailMatkulResponse {
  prodi_makul: string
  kode_makul: string
  nama_makul: string
  kelas_makul: string
  smt_makul: string
  teori_makul: string
  praktikum_makul: string
  dosen_makul: string
  deskripsi_makul: string
  jadwal: string
  detil: string

}